{
  "name": "portal-e2e",
  "version": "1.0.0",
  "description": "End-to-end tests for Multi-Cloud K8s Portal",
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "test:e2e": "start-server-and-test 'cd ../frontend && npm start' 3000 'cd ../backend && npm start' 3001 cypress:run"
  },
  "devDependencies": {
    "cypress": "^12.17.3",
    "cypress-plugin-api": "^2.11.1",
    "@testing-library/cypress": "^9.0.0",
    "start-server-and-test": "^2.0.0",
    "typescript": "^5.1.6",
    "@types/node": "^20.5.0"
  }
}
